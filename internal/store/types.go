package store

// types.go reserved for backend-agnostic helper types (transaction
// handles, option bundles) that are shared between sqlite and postgres
// implementations. Currently empty — see store.go for the main
// interface.
